# BankingPJ Frontend

React, TypeScript, Vite 기반의 BankingPJ 웹 클라이언트입니다.

## 로컬 실행

```powershell
npm install
npm run dev
```

API 주소는 `.env.example`을 참고해 `VITE_API_BASE_URL`로 설정합니다. `VITE_*` 값은 브라우저에 노출될 수 있으므로 비밀값을 넣지 않습니다.

## 검증

```powershell
npm test
npm run build
npm run lint
```
